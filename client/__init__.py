"""luduclone client: cross-OS backup/restore that talks to the server.

The same package runs on Windows (back up + upload) and Linux (download +
restore). OS-specific behavior is decided at runtime from the detected platform.
"""
