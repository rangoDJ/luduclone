"""Single source of truth for the client version.

Bump this when cutting a release tag (e.g. set to "0.1.9" alongside tag v0.1.9)
so the auto-updater compares correctly against the latest GitHub release.
"""

__version__ = "0.1.9"
